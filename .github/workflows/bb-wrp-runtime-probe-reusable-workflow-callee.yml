name: BugBase reusable workflow without Harden Runner

on:
  workflow_call:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Synthetic reusable marker
        run: echo "bb-wrp-runtime-reusable-workflow-dryrun-ms395mnp-reusable"
