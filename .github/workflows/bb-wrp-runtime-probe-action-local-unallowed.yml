name: BugBase WRP Allowed Actions action-local-unallowed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Allowed checkout prerequisite
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Unallowed local action
        uses: ./.github/actions/bb-wrp-action-policy-local
      - name: Marker after unallowed local action
        run: echo "bb-wrp-runtime-action-local-unallowed-enforce-ms3bu2w6"
