name: BugBase WRP Harden Runner Presence local-wrapper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout synthetic fixture
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Harden Runner
        uses: ./.github/actions/bb-wrp-runtime-probe-local-wrapper
      - name: Marker after local wrapper
        run: echo "bb-wrp-runtime-local-wrapper-dryrun-ms392t01"
