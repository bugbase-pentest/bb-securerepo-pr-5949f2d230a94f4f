name: BugBase WRP Harden Runner Presence reusable-workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-reusable:
    uses: ./.github/workflows/bb-wrp-runtime-probe-reusable-workflow-callee.yml
