name: BugBase WRP Allowed Actions action-allowed-tag

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Allowed checkout tag
        uses: actions/checkout@v4
      - name: Marker after allowed checkout tag
        run: echo "bb-wrp-runtime-action-allowed-tag-dryrun-ms3arbhx"
