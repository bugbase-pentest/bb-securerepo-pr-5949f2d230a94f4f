name: BugBase WRP Allowed Actions action-local-nested-unallowed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Allowed checkout prerequisite
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Local wrapper containing unallowed action
        uses: ./.github/actions/bb-wrp-action-policy-local
      - name: Marker after nested unallowed action
        run: echo "bb-wrp-runtime-action-local-nested-unallowed-enforce-ms3blmeb"
