name: BugBase WRP Allowed Actions action-allowed-sha

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Allowed checkout SHA
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - name: Marker after allowed checkout SHA
        run: echo "bb-wrp-runtime-action-allowed-sha-enforce-ms3bflb6"
