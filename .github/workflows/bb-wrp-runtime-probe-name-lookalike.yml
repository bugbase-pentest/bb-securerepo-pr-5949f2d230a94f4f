name: BugBase WRP Harden Runner Presence name-lookalike

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        run: echo "bb-wrp-runtime-name-lookalike-dryrun-ms37mdjm-name-only"
      - name: Marker after name-only lookalike
        run: echo "bb-wrp-runtime-name-lookalike-dryrun-ms37mdjm"
