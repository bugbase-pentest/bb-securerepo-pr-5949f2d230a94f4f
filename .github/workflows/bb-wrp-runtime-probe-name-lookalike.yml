name: BugBase WRP Harden Runner Presence name-lookalike

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  synthetic-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        run: echo "bb-wrp-runtime-name-lookalike-enforce-ms38ozdl-name-only"
      - name: Marker after name-only lookalike
        run: echo "bb-wrp-runtime-name-lookalike-enforce-ms38ozdl"
