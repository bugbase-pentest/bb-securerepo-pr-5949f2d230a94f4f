name: BugBase revision binding race-27fac8b13f58

on:
  workflow_dispatch:

jobs:
  binding:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "synthetic race-27fac8b13f58 reviewed-A"
