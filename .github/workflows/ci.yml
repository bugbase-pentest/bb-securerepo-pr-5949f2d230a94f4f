name: BugBase revision binding race-confirm-7dd473ee3564

on:
  workflow_dispatch:

jobs:
  binding:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "synthetic race-confirm-7dd473ee3564 changed-B"
